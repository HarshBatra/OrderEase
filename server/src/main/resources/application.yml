spring:
  application:
    name: SERVER

  datasource:
    url: jdbc:mysql://localhost:3306/<db-name>
    username: <db-username>
    password: <db-password>
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
